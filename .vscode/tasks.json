{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compose: stop adapter-svc",
            "type": "shell",
            "command": "docker compose stop adapter-svc",
            "options": { "cwd": "${workspaceFolder}" },
            "presentation": { "reveal": "silent", "panel": "shared" },
            "problemMatcher": []
        },
        {
            "label": "compose: stop gateway-svc",
            "type": "shell",
            "command": "docker compose stop adapter-svc",
            "options": { "cwd": "${workspaceFolder}" },
            "presentation": { "reveal": "silent", "panel": "shared" },
            "problemMatcher": []
        }
    ]
}
